import Header from '../components/Header';
import Hero from '../components/Hero';

const Home = function () {
  return (
    <section className='container'>
      <Header />
      <Hero />
    </section>
  );
};

export default Home;
